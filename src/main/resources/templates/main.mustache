<html>
<body>
<div>
    <form method="post">
        <input type="text", name="text", placeholder="Wpiś wiadomość">
        <input type="text", name="tag", placeholder="Tag">
        <button type="submit">Dodać</button>
    </form>
</div>
    <div>Lista wiadomościej</div>
    <form method="post" action="filter">
        <input type="text" name="filter">
        <button type="submit">Znaleść</button>
    </form>
    {{#messages}}
        <div>
            <b>{{id}}</b>
        <span>{{text}}</span>
            <i>{{tag}}</i>
        </div>
    {{/messages}}
</body>
</html>